module.exports = {
  webSocketURL: "ws://localhost:3000"
};
/*
  if websockets running on local machine, we need this because backend is running on port 3000 and frontend on port 8080
  this is only for development
*/
